name: Publish versioned HACS release

on:
  push:
    branches: [main]
    paths:
      - custom_components/totalplay_stb/manifest.json
      - .github/workflows/publish-release.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: totalplay-release
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check integration Python syntax and frontend import paths
        shell: bash
        run: |
          python -m compileall -q custom_components/totalplay_stb
          python - <<'PY'
          import re
          from pathlib import Path
          directory = Path('custom_components/totalplay_stb/www')
          for module in directory.glob('*.js'):
              source = module.read_text(encoding='utf-8')
              for dependency in re.findall(r'''\bimport\s*['"](\./[^'"]+\.js)(?:\?[^'"]*)?['"]''', source):
                  target = directory / dependency.removeprefix('./')
                  if not target.is_file():
                      raise SystemExit(f'Missing card dependency: {module.name} -> {dependency}')
          print('Python syntax and relative JavaScript imports checked')
          PY
          node --input-type=module --check < custom_components/totalplay_stb/www/totalplay-stb-v0372.js
          grep -F "TOTALPLAY CORPORATE IMAGE v0.3.72" custom_components/totalplay_stb/www/totalplay-stb-v0372.js
          grep -F -- "--tp-primary:#c52b6b" custom_components/totalplay_stb/www/totalplay-stb-v0372.js

      - name: Read and validate integration version
        id: version
        shell: bash
        run: |
          python - <<'PY'
          import json
          import os
          import re
          from pathlib import Path
          manifest = json.loads(Path('custom_components/totalplay_stb/manifest.json').read_text(encoding='utf-8'))
          version = manifest['version']
          if not re.fullmatch(r'[0-9]+\.[0-9]+\.[0-9]+', version):
              raise SystemExit(f'Expected version MAJOR.MINOR.PATCH, got {version!r}')
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f'tag=v{version}\n')
          print(f'Publishing (if missing): v{version}')
          PY

      - name: Publish GitHub release if missing
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
        run: |
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            echo "Release $RELEASE_TAG already exists; not replacing an immutable release."
            exit 0
          fi
          if git ls-remote --exit-code --tags origin "refs/tags/$RELEASE_TAG" >/dev/null 2>&1; then
            gh release create "$RELEASE_TAG" --verify-tag --title "$RELEASE_TAG" \
              --notes "Totalplay STB Local Remote $RELEASE_TAG. Experimental local HTTP remote control for Sagemcom DIW362 UHD; decoder command behavior has not yet been confirmed on physical hardware."
          else
            gh release create "$RELEASE_TAG" --target "$GITHUB_SHA" --title "$RELEASE_TAG" \
              --notes "Totalplay STB Local Remote $RELEASE_TAG. Experimental local HTTP remote control for Sagemcom DIW362 UHD; decoder command behavior has not yet been confirmed on physical hardware."
          fi
